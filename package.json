{
  "name": "socketio-chat",
  "version": "1.0.0",
  "description": "Chat en tiempo real utilizando Socket IO",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chat",
    "socketio",
    "nodejs"
  ],
  "author": "Franco Juárez",
  "license": "ISC",
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "dotenv": "^16.4.7",
    "express": "4.21.2",
    "morgan": "1.10.0",
    "socket.io": "4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "standard": "17.1.2"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}